{
  "scripts": {
    "start": "snowpack dev",
    "watch": "tsc --watch",
    "build-browser": "snowpack build",
    "build": "tsc",
    "test": "node --enable-source-maps --trace-deprecation --unhandled-rejections=strict build/src/test.js",
    "format": "prettier --write \"src/**/*.{ts,js}\"",
    "lint": "prettier --check \"src/**/*.{ts,js}\""
  },
  "dependencies": {},
  "devDependencies": {
    "@snowpack/plugin-run-script": "^2.2.0",
    "@types/snowpack-env": "^2.3.2",
    "prettier": "^2.2.0",
    "snowpack": "^2.17.1",
    "typescript": "^4.1.2"
  }
}
